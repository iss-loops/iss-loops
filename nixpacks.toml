[phases.setup]
nixPkgs = ['php82', 'php82Extensions.intl', 'php82Extensions.zip']

[phases.install]
cmds = ['composer install --no-dev --optimize-autoloader --no-scripts --no-interaction']

[start]
cmd = 'php artisan migrate --force && php artisan optimize && php artisan serve --host=0.0.0.0 --port='
